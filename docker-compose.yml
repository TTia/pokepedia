services:
  pokepedia:
    build: .
    ports:
      - "${HOST_PORT:-5000}:${SERVER_PORT:-5000}"
    environment:
      - POKEAPI_BASE_URL=${POKEAPI_BASE_URL:-https://pokeapi.co/api/v2}
      - SERVER_PORT=${SERVER_PORT:-5000}
    restart: unless-stopped
